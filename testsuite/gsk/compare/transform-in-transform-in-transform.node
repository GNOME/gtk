transform {
  child: transform {
    child: transform {
      child: color {
        bounds: 0 0 20 20;
        color: blue;
      }
      transform: translate(0, -20);
    }
    transform: rotate(90);
  }

  transform: translate(300, 200);
}
